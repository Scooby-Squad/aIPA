diff a/rn-client/screens/SingleBeerView.js b/rn-client/screens/SingleBeerView.js	(rejected hunks)
@@ -5 +5 @@ import StarRating from 'react-native-star-rating';
-import Hyperlink from 'react-native-hyperlink'
+import Hyperlink from 'react-native-hyperlink';
@@ -19 +19 @@ const styles = StyleSheet.create({
-    margin: 25,
+    margin: 25
@@ -23 +23 @@ const styles = StyleSheet.create({
-    fontSize: 19,
+    fontSize: 19
@@ -39 +39 @@ const styles = StyleSheet.create({
-    justifyContent: 'space-around',
+    justifyContent: 'space-around'
@@ -44 +44 @@ const styles = StyleSheet.create({
-    fontSize: 19,
+    fontSize: 19
@@ -54,8 +54,10 @@ export default function Single(props) {
-  useEffect(() => {
-    const dispatchers = async () => {
-      await dispatch(updateUserBeer(data));
-      await dispatch(getRankedBeers());
-    };
-    dispatchers();
-  }, [data]);
-
+  useEffect(
+    () => {
+      const dispatchers = async () => {
+        await dispatch(updateUserBeer(data));
+        await dispatch(getRankedBeers());
+      };
+      dispatchers();
+    },
+    [data]
+  );
@@ -68,2 +70,2 @@ export default function Single(props) {
-    await dispatch(addToWishlistThunk(beer))
-    await setData({...data, rating: "0"})
+    await dispatch(addToWishlistThunk(beer));
+    await setData({ ...data, rating: '0' });
@@ -73,3 +75,3 @@ export default function Single(props) {
-    await dispatch(removeFromWishlistThunk(beer))
-    await setData({...data, rating: null})
-  }
+    await dispatch(removeFromWishlistThunk(beer));
+    await setData({ ...data, rating: null });
+  };
@@ -80,6 +82,12 @@ export default function Single(props) {
-              source={
-                __DEV__
-                  ? require('../assets/images/single-beer.png')
-                  : require('../assets/images/single-beer.png')
-              }
-              style={{width: '110%', height: '110%', bottom: 0, position: 'absolute', opacity: 0.6}}
+        source={
+          __DEV__
+            ? require('../assets/images/single-beer.png')
+            : require('../assets/images/single-beer.png')
+        }
+        style={{
+          width: '110%',
+          height: '110%',
+          bottom: 0,
+          position: 'absolute',
+          opacity: 0.6
+        }}
@@ -125 +133,5 @@ export default function Single(props) {
-          <Text style={styles.text}>{data.description.length > 600 ? '    ' + data.description.slice(0, 600).concat('...') : '    ' + data.description }</Text>
+          <Text style={styles.text}>
+            {data.description.length > 600
+              ? '    ' + data.description.slice(0, 600).concat('...')
+              : '    ' + data.description}
+          </Text>
@@ -130 +142,4 @@ export default function Single(props) {
-          <Hyperlink linkDefault={ true } linkStyle={ { color: 'blue', fontSize: 20 }}>
+          <Hyperlink
+            linkDefault={true}
+            linkStyle={{ color: 'blue', fontSize: 20 }}
+          >
@@ -163 +178 @@ export default function Single(props) {
-      {data.rating === "0" ? (
+      {data.rating === '0' ? (
